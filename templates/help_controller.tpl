Usage:
    caffeine controller [controller] [module]

Description:
    Creates a new Caffeine controller for the given module
    within the current project. The module must already exist.

Example:
    caffeine controller posts blog
