# Sites should have their own git repo
# Ignore them
sites/

# The files directory is for dynamically uploaded content
# We dont need to track those files, so ignore them
files/

# Extras
*.swp
.DS_Store
